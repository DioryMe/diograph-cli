const listCommand = (subcommand: string) => {
  switch (subcommand) {
    case 'rooms':
      break
    case 'connections':
      break
    case 'connectionContents':
      break
    default:
      break
  }
}

export { listCommand }
