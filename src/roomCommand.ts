const roomCommand = (subcommand: string) => {
  switch (subcommand) {
    case 'create':
      break
    case 'remove': // from app
      break
    case 'delete':
      break
    case 'focus':
      break
    default:
      break
  }
}

export { roomCommand }
